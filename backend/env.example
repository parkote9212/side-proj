# ========================================
# Backend Environment Variables Example
# ========================================
# 이 파일을 복사하여 .env 파일을 생성하고 실제 값을 입력하세요.
# .env 파일은 .gitignore에 포함되어 있어 Git에 커밋되지 않습니다.

# ========================================
# JWT 설정
# ========================================
# JWT Secret Key (최소 256비트 권장, 32자 이상)
# 프로덕션에서는 반드시 강력한 랜덤 문자열 사용
JWT_SECRET_KEY=your-super-secret-jwt-key-minimum-32-characters-long

# JWT 토큰 만료 시간 (밀리초, 기본값: 3600000 = 1시간)
JWT_EXPIRATION_TIME=3600000

# ========================================
# 서버 설정
# ========================================
# 서버 포트 (기본값: 8080)
SERVER_PORT=8080

# ========================================
# 데이터베이스 설정
# ========================================
# 데이터베이스 URL
SPRING_DATASOURCE_URL=jdbc:mariadb://localhost:3306/onbid_db?characterEncoding=UTF-8&serverTimezone=Asia/Seoul

# 데이터베이스 사용자명
SPRING_DATASOURCE_USERNAME=root

# 데이터베이스 비밀번호 (프로덕션에서는 반드시 강력한 비밀번호 사용)
SPRING_DATASOURCE_PASSWORD=your_database_password

# ========================================
# 외부 API 설정
# ========================================
# Onbid API Base URL
ONBID_API_BASEURL=http://openapi.onbid.co.kr/openapi/services/KamcoPblsalThingInquireSvc

# Onbid API Service Key
ONBID_API_SERVICEKEY=your_onbid_api_service_key

# Kakao API Base URL
KAKAO_API_BASEURL=https://dapi.kakao.com

# Kakao API REST API Key
KAKAO_API_RESTAPIKEY=your_kakao_api_rest_api_key

# ========================================
# CORS 설정
# ========================================
# 허용할 프론트엔드 Origin (쉼표로 구분)
# 예: http://localhost:5173,https://yourdomain.com
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# ========================================
# Spring Profile
# ========================================
# 활성화할 프로파일 (dev, prod 등)
# 예: SPRING_PROFILES_ACTIVE=dev
SPRING_PROFILES_ACTIVE=dev
